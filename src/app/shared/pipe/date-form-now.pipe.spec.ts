import { DateFormNowPipe } from './date-form-now.pipe';

describe('DateFormNowPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormNowPipe();
    expect(pipe).toBeTruthy();
  });
});
